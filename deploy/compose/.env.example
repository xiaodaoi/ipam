# 复制为 .env 后修改；.env 不入库
IPAM_PORT=8443
IPAM_VERSION=0.1.0-dev

POSTGRES_DB=ipam
POSTGRES_USER=ipam
POSTGRES_PASSWORD=change-me-strong
KEA_API_PORT=8000
UNBOUND_PORT=5353
# 中国网络环境启用（http 而非 https，规避容器内证书问题）：
#   APT_MIRROR=http://mirrors.aliyun.com
# Go 工具链下载源（中国网络）：GO_DL=https://golang.google.cn
APT_MIRROR=
GO_DL=
