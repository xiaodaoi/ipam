# [M2-001] 组织分组 API（org_group 多级树）

| ID | M2-001 | 状态 | done | 来源 | §13.4 主数据原则 |
|---|---|---|---|---|---|
| 负责 | opencode(backend) | 创建 | 2026-08-26 |

## 目标
spec 先行：org 分组 CRUD（list 树形/创建/改名/移动/删除保护）→ 生成 → 实现 internal/module/ipam/org。

## 验收标准
- [ ] 删除含子节点或被 subnet/asset 引用时返回 RFC9457 409 ORG_IN_USE
- [ ] list 返回物化路径树；operationId 全驼峰；双 example

## DoD
单测（树操作+删除保护）/ lint / §13.4 同步 / spec 先行 ✓ / commit [M2-001]

## 实施记录

### 2026-08-26 · 会话1
- **做了**：spec 先行——orgs 四端点（listOrgTree/createOrg/updateOrg/deleteOrg）含双 example、x-risk-level、scope 声明；多文件引用经 redocly bundle 后生成（gen 管线升级）；internal/module/ipam 落地 MemOrgStore+OrgService（物化路径/环检测/级联刷新/删除保护/改名冲突）+ OrgHandler；problem 包下沉解循环依赖；control-plane 组合 handler。
- **验证**：8 单测全绿（路径嵌套/重名/删除保护×2/环检测/级联刷新/树排序）；build/vet/golangci-lint 0 issues；闸②路由覆盖率自动覆盖新路由。
- **踩坑留痕**：① oapi-codegen 多文件引用以**引用方文件目录**为基准，递归自引用用 '#/X' 本地片段，跨文件 Problem 引用改为内联同构 schema；② 生成代码 uuid 参数为 rtypes.UUID 类型需 MustParse 转换；③ 双域 handler 嵌入字段名冲突→org 域命名 OrgHandler；④ gin ShouldBindJSON 用于非严格模式的 body 绑定。
- **遗留**：① MemOrgStore 为 PoC 实现，pgx 持久化仓储随 M2-002 子网表同一迁移批次落地；② bearerAuth 中间件在 RBAC 里程碑(M5)统一接线，当前 spec 已声明契约。
