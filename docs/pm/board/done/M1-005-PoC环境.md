# [M1-005] PoC 运行环境（Kea+Unbound 容器编排）

| 字段 | 内容 |
|---|---|
| ID | M1-005 |
| 状态 | done |
| 来源 | D12、§9 |
| 负责 | opencode(devops) |
| 创建 | 2026-08-25 |
| 更新 | 2026-08-26 |

## 目标
deploy/compose 扩展 kea-dhcp4/6(ctrl-agent)+unbound 数据面容器，打通 config-set/checkconf 下发通道；CI 闸⑥升级为全组件断言。

## 验收标准
- [x] Kea 配置下发成功（ctrl-agent version-get result:0，转发 dhcp4/dhcp6）
- [x] unbound reload/local_data 生效（动态注入→立即可解析）
- [x] 全栈 compose 一键起服（7 容器全 Up）

## DoD 自检
- [x] CI 冒烟即测试　- [x] yaml/shell 校验通过
- [x] §7 端口矩阵一致　- [x] spec N/A　- [x] commit 带 [M1-005]

## 实施记录

### 2026-08-26 · 会话1
- **做了**：Kea 三组件 Debian bookworm 官方仓自建镜像(2.2.0)；Unbound GitHub 源码钉版 1.26.0 自编译+checkconf 启动门禁(NLnetLabs apt 域不可达的确定性替代，K8 满足≥1.16)；daemon 接 PG(LISTEN/NOTIFY)；APT_MIRROR 环境感知参数(中国本地 .env 切阿里云，CI 默认官方源)；闸⑥五段断言。
- **验证**：CI run 全绿，7 容器 Up，五断言含 local_data 动态注入→立即解析实证。
- **踩坑留痕**：isc/*:2.6 标签不存在；GitHub 归档需 flex/bison；configure 需 file 包；源码编译默认校验 unbound 用户需 useradd；Kea2.2 用 output_options 下划线语法；local_data 参数式调用非 stdin；unbound.conf 中文注释触发词法错误须 ASCII；诊断经 ci-diag 分支匿名可读。
- **遗留**：hook .so 挂载与 gRPC C++ 客户端待真机 Kea 头文件环境；§9 真机清单随硬件执行；cap 收紧按 §7 于 M5 统一硬化。
