# [M0-005] CI 流水线六道闸

| 字段 | 内容 |
|---|---|
| ID | M0-005 |
| 状态 | done |
| 来源 | §12.4、§13.3 |
| 负责 | opencode(devops) |
| 创建 | 2026-08-25 |
| 更新 | 2026-08-25 |

## 目标

搭建 CI 流水线：① spectral lint ② 路由覆盖率(spec⊇Gin routes) ③ openapi-diff ④ go test+golangci-lint ⑤ web 构建零外链断言 ⑥ compose 冒烟。

## 验收标准

- [x] 六道闸全部可独立触发且失败即红（⑥ 在 M0-006 落地前自动 skip，落地即自动生效）
- [x] main 保护规则——**待人工在 GitHub 网页启用**（Settings→Branches→Require PR）
- [x] 各闸失败输出定位信息（spectral 行级报告 / go test 用例名 / oasdiff 变更明细）

## 涉及模块

- .github/workflows/ci.yml、.spectral.yaml、.golangci.yml、Makefile(lint-api)、cmd/control-plane/main_test.go(闸②测试)

## DoD 自检

- [x] 闸②以 Go 测试实现并本地绿；闸①④⑤本地预跑全绿
- [x] yaml lint：人工走查 + Actions 首跑验证
- [x] 文档同步：§12.4 命令落地注记于本卡
- [x] spec N/A　- [x] commit 带 `[M0-005]`

## 实施记录

### 2026-08-25 · 会话1

### 2026-08-25 · 首跑验收

- 第二次运行全绿（32815471234），卡片转 done。

- **做了**：
  - 闸②实现为 `TestRoutesCoveredBySpec`（嵌入 spec GetSwagger() vs gin Routes() 全量比对，gin 通配→OpenAPI 模板转换已内置）；
  - `.spectral.yaml`：oas 推荐集 + 文档完备性三件套升 error + examples 强制 + operationId 驼峰规则；
  - `.golangci.yml` v2 最小配置（api/gen 禁改区排除）；
  - `ci.yml` 五 job 编排：api-lint / go-ci(含 stub webui 保证 fresh clone 测试有效) / openapi-diff(PR 对基线) / web-ci(install→typecheck→build→sync→offline 断言) / compose-smoke(M0-006 前自跳过)；
  - Makefile 新增 lint-api 目标。
- **本地验证**：spectral 无 error；go build/vet/test 全绿（4 用例）。
- **踩坑留痕**：① spectral 内置无 operation-summary 规则名 → 自定义 operation-has-summary；② kin-openapi GetOperation 要求大写方法名；③ 切勿钉旧版 kin-openapi（生成代码按 v0.147 API 产出）；④ CI 中 fresh clone 缺前端产物会让 embed fallback 测试失真 → workflow 内置 stub index.html 步骤。
- **遗留**：① 首次 Actions 运行结果跟踪（推送后进行中，结果见仓库 Actions 页）；② golangci-lint 版本在 CI 用 @latest，首跑若配置不兼容则回退固定版本；③ main 分支保护需人工网页操作。
