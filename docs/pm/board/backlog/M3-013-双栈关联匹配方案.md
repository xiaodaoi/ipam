# [M3-013] 双栈关联匹配方案（matchScheme，ADR D16 / §4.5）

| 字段 | 内容 |
|---|---|
| ID | M3-013 |
| 状态 | backlog |
| 来源 | 架构文档 §4.5 + ADR D16（老旧交换机不支持 option79 的兼容方案） |
| 负责 | opencode(backend+frontend+devops) |
| 创建 | 2026-09-10 |
| 更新 | 2026-09-10 |

## 目标

把双栈 MAC↔DUID 关联方式抽象为模板级可选项 `matchScheme`，option79 为 auto 链首选，并提供老旧交换机可用的降级（client-id / duid-llt / hostname）与人工兜底（admin 映射 + kea6 预留），使客户端首次分配即得规范 v6。

## 验收标准（可测）

- [ ] `prefix_template.match_scheme` 迁移 + 枚举校验；存量模板 = auto（零破坏）
- [ ] `dualstack_identity` 表 + 精确方式自动学习落表（hostname 不落表）
- [ ] 对账器方案链：admin→option79→client-id→duid-llt→hostname→冲突；钉死方案拦截跨方案回落
- [ ] kea6 预留下发（reservation-add）→ 首次 Solicit 即得规范地址（合成租约 e2e 断言）
- [ ] UI：模板表单方案下拉 + 冲突清单 + 一键建映射 + 列表方案列
- [ ] 单测覆盖六方案解析 + 链序 + 钉死拦截；回归（ApplyTemplate B/A、hostname 空值回查、同名不猜）全绿
- [ ] 文档三件套 + commit 带 [M3-013]

## 涉及模块

- `db/postgresql/migrations/`（match_scheme 列 + dualstack_identity 表）
- `internal/module/coherence/dualstack_reconcile.go`（方案链重构）
- `internal/module/dualstack/`（模板 CRUD 加字段 + 冲突清单 API）
- `hook-coherence/` + `deploy/images/kea/Dockerfile`（option79 hook 编译进镜像）
- `config/kea/` + `internal/engine/kea/config.go`（hook 加载）
- `api/openapi/`（模板字段 + 冲突/映射端点）
- `web/apps/web-ipam/src/views/dhcp/dualstack/`（方案下拉 + 冲突清单）

## DoD 自检（完成前逐条勾选）

- [ ] 核心逻辑有单测
- [ ] lint/typecheck 通过
- [ ] 相关文档章节已同步（§4.5 已定稿；实现差异回写）
- [ ] API 变更已先行修改 spec（如涉及）
- [ ] commit 带 `[M3-013]`

## 实施记录（追加式，勿删旧条目）

### 2026-09-10 · 设计定稿

- **做了**：方案研讨定稿并写入架构文档 §4.5 + ADR D16；拆解实施任务 T1–T7（见 §4.5 与设计讨论）。
- **前置条件**：option79 方案需网络侧中继开 RFC6939 + 本仓库 kea hook 编译进镜像（当前 hook 源码在 `hook-coherence/`，镜像未编译、kea6 未加载——已核实）。
- **现状**：现有 hostname 对账器（dualstack_reconcile.go）已达成 B 型镜像（10.193.135.15 → 2406:440:3c16:4006:10:193:135:15），本卡将其升级为方案链 + 预留收尾。
- **遗留**：待确认钉死语义（仅认该方案 vs 优先降级）、admin 恒最优先两项决策后开工。
