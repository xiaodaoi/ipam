# [M3-001] 上游 DNS 管理 API + 探活 prober

| ID | M3-001 | 状态 | review | 来源 | FR-B-01、F-R4、§13.4 上游管理 |
|---|---|---|---|---|---|
| 负责 | opencode(backend) | 创建 | 2026-08-26 |

## 目标
upstream CRUD（addr/权重/协议）+ 健康探测循环（连续失败摘除、恢复回切）+ forward-zone 下发到 unbound；UI 探活状态灯数据源。

## 验收标准
- [ ] prober：TCP:53 探测，3 连败摘除/2 连胜回切；RTT 记录
- [ ] 下发：unbound-control forward_add/forward_remove 或 reload；不可达时 ErrUnboundUnavailable 语义
- [ ] 状态查询端点返回 up/down/rtt/权重

## DoD
单测（探活状态机/下发/CRUD）/ lint / §13.4 同步 / spec 先行 / commit [M3-001]

## 实施记录

### 2026-08-26 · 会话1
- **做了**：spec 先行——upstream 三端点（list 含 health/create/update/delete，dns 域 tag）；迁移 0004；internal/module/dns（Upstream CRUD+Prober 状态机：TCP:53 探测、3 连败摘除/2 连胜回切、RTT/LastCheck；Service 创建/更新后 forward-zone 重下发）；internal/engine/unbound（ExecController forward_add/forward_remove、normalizeAddr 补端口、DialProbe）；Pg/Mem 双仓储；DnsHandler 接线（命名避开 platform.Handler 冲突）。
- **验证**：dns+unbound 4 新单测全绿（状态机/服务下发/地址归一化/缺二进制语义）；全仓 7 包 lint 0 issues。
- **踩坑留痕**：dns.Handler 与 platform.Handler 嵌入冲突→DnsHandler 命名；health 为值类型 struct 非指针；openapi_types.UUID 直传参数。
- **遗留**：协议扩展（DoT 探测 P2）；RTT 权重参与 forward 排序（P1）；UI 探活灯接入 M3 前端卡；forward_add 幂等性在闸⑥容器实测（M3-006）。
