# [M2-021] 编辑闭环批次（上游/转发规则/选项类）

| 字段 | 内容 |
|---|---|
| ID | M2-021 |
| 状态 | review |
| 来源 | 编辑闭环盘点：三页缺编辑（API+后端全就绪，仅前端缺表单）——沿 M2-020 模式 |
| 负责 | opencode(frontend) |
| 创建 | 2026-08-28 |
| 更新 | 2026-08-28 |

## 方案

三页复用创建表单为编辑模式（editingId + 行编辑按钮 + 表单预填 + PATCH 提交）：
1. 上游管理：updateUpstream（地址变更 → forward-zone 重收敛 ✓ 后端已有）
2. 转发规则：updateForwardRule（spec/后端已有）
3. 选项类：updateDhcpOption/updateDhcpClass（M2-016 API ✓，afterApply ✓）

## 验收标准（可测）

- [x] 三页编辑模式（行编辑按钮 + 表单预填 + PATCH）；typecheck/零外链 PASS
- [x] e2e：上游 PATCH 改地址 → 回读一致；转发 PATCH 改 note → 回读一致；选项 PATCH 改 data → 回读一致
- [x] commit [M2-021]

## 实施记录（追加式，勿删旧条目）

### 2026-08-28 · 会话1
- **做了**：三页编辑模式（editingId + edit() 预填 + PATCH/POST 分支 + 提交按钮动态文案 + 取消编辑按钮 + op 编辑按钮）；ipam.ts updateForwardRule（前端 API 补齐，spec/后端已有）。
- **验证结果**：typecheck 0 + 零外链 PASS + 全链测试绿 + lint 0；容器 e2e——上游 PATCH（223.6.6.6→223.7.7.7）200+回读一致、转发 PATCH（note）200+回读一致、选项 PATCH（routers data）200+回读一致。
- **踩坑留痕**：① TS2345 ×3（Table slot 的 record 类型收窄——as 断言）；② python heredoc anchor 与实际文本缩进差异多次静默失败 → Edit 工具精确锚定。
- **遗留**：双栈页编辑（spec 无 update——删建模式可接受）P2；封禁条目编辑（条目增删已有）P2。
