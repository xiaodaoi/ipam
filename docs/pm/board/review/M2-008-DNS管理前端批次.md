# [M2-008] DNS 管理前端批次（上游/转发/记录/封禁）

| 字段 | 内容 |
|---|---|
| ID | M2-008 |
| 状态 | review |
| 来源 | §13.4 DNS 服务六菜单（四页 CRUD 批次；缓存/安全与诊断页属 P1 批次） |
| 负责 | opencode(frontend) |
| 创建 | 2026-08-28 |
| 更新 | 2026-08-28 |

## 目标
消费 M3 已交付的 DNS API：上游管理（探活状态灯+CRUD）、转发规则（条件转发+dryRun）、解析记录（zone 选择+记录 CRUD+联动只读）、封禁管控（名单+订阅同步）。「DNS 服务」一级菜单四子页。

## 验收标准
- [ ] 上游页：列表含探活状态，增删改生效
- [ ] 转发页：规则列表+新增（域名后缀+上游）；删除
- [ ] 记录页：zone 下拉切换，记录增删，联动记录只读 tab
- [ ] 封禁页：名单列表+同步状态与手动刷新
- [ ] typecheck/build/零外链 PASS

## 涉及模块
- web/apps/web-ipam/src/{api,views/dns,router,locales}

## DoD 自检
- [ ] 核心逻辑有单测（N/A 纯前端；typecheck 承担）
- [ ] lint/typecheck 通过
- [ ] 文档同步（进度日志）
- [ ] API 已先行存在（M3 交付）
- [ ] commit 带 [M2-008]

## 实施记录（追加式，勿删旧条目）

### 2026-08-28 · 会话1
- **做了**：api/ipam.ts 扩 DNS 域 12 函数；上游页（探活状态灯+RTT+15s 刷新+增删）、转发规则页（域名后缀→上游选择+增删）、解析记录页（zone 下拉+静态记录 CRUD+联动只读 tab+新建 zone）、封禁页（名单列表+feed 立即同步+30s 刷新）；「DNS 服务」一级菜单四子页+zh/en locale。
- **踩坑**：生成类型的 recType/protocol 为字面量联合，表单 ref 需显式标注类型（Partial<> 入参约束）。
- **验证结果**：typecheck/build(2m53s 全量)/零外链 PASS；golangci 0。
- **遗留**：缓存与性能/安全诊断两页 P1 批次；封禁策略分组视图 P1。
