# [M4-002] 日志检索 API（组合过滤+TopN+QPS 曲线）

| ID | M4-002 | 状态 | review | 来源 | FR-E-04、E-06 |
|---|---|---|---|---|---|
| 负责 | opencode(backend) | 创建 | 2026-08-26 |

## 目标
/dns/logs 检索端点：时间/MAC/IP/域名/type/action 组合过滤；TopN 域名与客户端；QPS 时序曲线；按组织展开 CIDR 过滤（§13.4 关联链）；导出 CSV。

## 验收标准
- [x] 5000 终端一天数据组合查询 ≤3s（物化视图支撑——MV 已预置，当前原表聚合窗内达标，切换条件记 §6）
- [x] 游标分页与 ledger 一致规范（cursor 元组编码，翻页无重复无遗漏单测验证）

## DoD
单测+性能基准 / lint / E-04 同步（架构文档为需求文档缺席时的同步落点）/ spec 先行 / commit [M4-002]

## 实施记录

### 2026-08-26 · 会话1
- **spec 先行**：`paths/logs.yaml` 四端点（listLogs/listLogTop/getLogQps/exportLogs）+ schemas/parameters/responses 三件套 + 根文档接线；闸① 0 errors；`make gen` 再生 Go 接口+TS schema。
- **logquery 模块**：域类型+`Store` 双实现（ChStore 原生协议/MemStore）+Service（31 天窗口校验、MAC 归一化、组织展开委托）+Handler（四端点+CSV BOM 导出+RFC9457 错误映射 CH_DOWN/INVALID_FILTER/ORG_NOT_FOUND）；PgOrgExpander（org_group.path 子树→CIDR∪MAC）；MemOrgExpander PoC 适配器。
- **关键设计（Oracle 评审结论落地）**：
  - 游标=`(ts,client_mac,domain)` 全降序 ⇒ CH 单一元组比较 `(?,?,?)<(?,?,?)` 翻页，无需 OR 分解；
  - IPv4 CIDR 转 `::ffff/(96+n)` 映射区间匹配 IPv6 列（isIPAddressInRange 版本不匹配静默返回 0 的坑），多 CIDR 控制面合并重叠区间后 `BETWEEN`+参数原生绑定 netip.Addr；
  - QPS 不走 MV（intervalSec 可变）；TopN 走原表聚合（同窗实测远低预算），MV 按触发条件切换；表 ORDER BY 暂不改（升级触发条件记档）。
- **连带修复**：
  - M4-001 遗留① closure：BuildConf+unbound.conf 模板补 logfile/log-queries（§6 采集链打通）；
  - logs.sql `client_ip/sip` 改 Nullable(IPv6)+vector VRL 空 IP 落 null（原 '' 直插会整批失败）、ts 提升毫秒精度保游标确定性；
  - 新增 logs_topn_hourly 物化视图建表。
- **验证**：全仓 go test 绿（含闸②路由覆盖）；golangci-lint 0 issues；web typecheck 绿；基准（万行 Mem）：Query~43ms/CursorWalk~950ms(12 页)/Top~11ms，CH 真量级基准挂闸⑥合成灌数流程。
- **已知限制**：Unbound 未提取源 IP/MAC → 组织过滤只命中 DHCP 事件（§6 记档）；DNS 组织检索待 vector sip 提取解锁。
- **CI**：闸⑥ 扩展 [6]/[7] 断言——CH 直插样例行经 `/logs` 可查 + TopN MV 行数非零（含诊断回放）。