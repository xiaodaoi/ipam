# [M5-012] 令牌黑名单持久化

| 字段 | 内容 |
|---|---|
| ID | M5-012 |
| 状态 | review |
| 来源 | M5-011 遗留：内存黑名单进程重启清空——登出令牌在重启后复活至自然过期，真实安全缺口 |
| 负责 | opencode(backend) |
| 创建 | 2026-08-29 |
| 更新 | 2026-08-29 |

## 方案

最小改动（启动加载 + 双写 + 内存查询）：
1. 迁移 0015：`auth_token_blacklist(token_hash PK, until TIMESTAMPTZ)` + until 索引
2. TokenBlacklist.AttachDB(pool)：清理过期条目 + 加载未过期条目进内存 + 启用 Add 双写（best-effort）
3. Revoked 仍走内存（启动加载保证重启后有效，单实例语义）
4. main.go：pool != nil 时 AttachDB（失败降级内存 + 日志）

## 验收标准（可测）

- [x] 迁移 0015 由 runner 自动应用
- [x] e2e 决定性：登录 → 登出（401 TOKEN_REVOKED）→ **重启 control-plane** → 同令牌**仍 401 TOKEN_REVOKED** → 新登录 200
- [x] 全链绿（build/test/lint/typecheck）+ 文档三件套 + commit [M5-012]

## 实施记录（追加式，勿删旧条目）

### 2026-08-29 · 会话1
- **做了**：迁移 0015（auth_token_blacklist 表 + until 索引）；TokenBlacklist.AttachDB（DELETE 过期 + SELECT 加载未过期 + 启用 db 字段）；Add best-effort 双写（INSERT ON CONFLICT DO UPDATE，失败仅影响重启恢复窗口、内存即时生效）；main.go pool 非空时 AttachDB（失败降级内存 + [bl-load] 日志）。
- **验证结果**：platform 测试 ok + lint 0 + go build ✓；e2e 决定性证据——登出 200 → 同令牌 401 → **docker compose restart control-plane → 同令牌仍 401 TOKEN_REVOKED**（内存黑名单重启即清空，401 只能来自 AttachDB 的 DB 加载——重启恢复语义闭环）→ 新登录 200。
- **踩坑留痕**：postgres 容器名非 ipam-postgres-1（诊断用 psql 需先 docker ps 确认实际容器名）。
- **遗留**：多实例语义已由 **M5-031** 闭环（Revoked 直查 PG 点查，不引入 Redis——架构评估见该卡）；refresh token 机制列远期演进。
