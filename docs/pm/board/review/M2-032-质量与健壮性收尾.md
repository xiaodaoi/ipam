# [M2-032] 质量与健壮性收尾（回归锁 + 文档同步 + 日志限额）

| 字段 | 内容 |
|---|---|
| ID | M2-032 |
| 状态 | review |
| 来源 | 出口前回归暴露的三个真实弱点：① 回归抓到的 bug（Command result-3、NewDnsHandler 签名连带）无单测锁定——测试面不足；② B-10 已实现但架构文档 §571 仍标 P2——文档同步纪律欠账；③ ENOSPC 两次根因之一是容器 stdout 日志无限膨胀（json-file 无上限） |
| 负责 | opencode(backend+devops) |
| 创建 | 2026-08-29 |
| 更新 | 2026-08-29 |

## 方案

1. **回归锁单测 ×3**：kea Command result-3 容忍（httptest 假 agent）；blocklist MemRepo DeleteList 级联 + DeleteEntry 自然键/not-found；MatchPolicyView CIDR 五分支（命中/多段/不匹配/坏 IP/坏 CIDR）——**抽为包级函数**（main 闭包去重改调）
2. **架构文档 §571**：B-10 由「P2 占位」改「已落地 M2-030」
3. **compose 日志限额**：6 个 image 型服务 json-file max-size=10m/max-file=3

## 验收标准（可测）

- [x] 三组单测绿（新增 5 断言覆盖回归 bug 场景）
- [x] 全量 go test/lint/typecheck 绿 + compose config OK + 全栈重建冒烟 5 端点 200 + inspect 实收日志限额
- [x] 文档三件套 + commit [M2-032]

## 实施记录（追加式，勿删旧条目）

### 2026-08-29 · 会话1
- **做了**：① 回归锁：ctrl_test result-3 空结果容忍测试；blocklist_test DeleteList 级联/DeleteEntry 自然键与 not-found/MatchPolicyView 五分支；MatchPolicyView 抽为 blocklist.go 包级函数（main.go policyViewFn 去重改调，net/netip import 移除）。② sentinel 统一：删除 M2-024 引入的小写重复定义（8 refs + handler 漏网 1 处 → 导出 ErrBlocklistNotFound）。③ 架构文档 §571 B-10 同步。④ compose.yaml 6 服务 logging 限额。
- **验证结果**：全量 go test 绿（新增断言全 PASS）+ lint 0 + typecheck 0 + sync/零外链 PASS；compose config OK + `up -d --force-recreate` 全栈重建后冒烟 5 端点 200 + `docker inspect` 实收 `map[max-file:3 max-size:10m]`。
- **踩坑留痕**：① **gofmt 会重排 import 字母序**（M2-031 写入的 net/netip 被排到 net/http 后）——锚点按写入形态失配，**锚点前必须重看现场**；② 同文本双 sentinel（大写导出 + 小写未导出）统一时的 self-replace 笔误漏 handler 一处——**替换前后必须不同**。
- **遗留**：vector/coherence-daemon 为 build 构建型服务（无 image 行）未覆盖日志限额，后补 P3。
