# [M2-014] DNS 解析测试台真实现

| 字段 | 内容 |
|---|---|
| ID | M2-014 |
| 状态 | review |
| 来源 | M2-011 遗留 P1；§13.4 DNS 服务 6/6 安全与诊断 |
| 负责 | opencode(backend/frontend) |
| 创建 | 2026-08-28 |
| 更新 | 2026-08-28 |

## 目标

安全与诊断页的解析测试台从占位变真实现：选定记录名/类型，从 control-plane 向 unbound 发起 dig 式查询，结构化返回 rcode/答案/RTT。

## 验收标准（可测）

- [ ] spec /dns/diagnose POST（闸① 0 errors）+ gen
- [ ] 后端 stdlib net.Resolver 实现（零新依赖，许可安全）；查询目标 env `IPAM_DNS_DIAG_ADDR`（默认 unbound:53）
- [ ] 返回结构化：rcode、answers（name/type/TTL/value）、rttMs、server
- [ ] 前端安全页占位换表单+结果面板；typecheck/build/零外链 PASS
- [ ] 容器 e2e：POST /dns/diagnose → 200 结构化 JSON
- [ ] lint 0、单测绿、commit [M2-014]

## 涉及模块

- `api/openapi/paths/diagnose.yaml` + `components/schemas/diagnose.yaml`
- `internal/module/dns/diagnose.go`（新增 Service 方法+handler）
- `web/apps/web-ipam/src/views/dns/security/index.vue`

## 实施记录（追加式，勿删旧条目）

### 2026-08-28 · 会话1
- **做了**：spec /dns/diagnose POST（闸① 0 errors）+ gen；internal/module/dns/diagnose.go——miekg/dns（BSD-2，已登记 LICENSES.md）构造查询，stdlib netip 做 PTR 反查（IPv4 直接填 IP 便利形态；IPv6 As16 逐 nibble 逆序展开，零压缩写法正确）；Service.Diagnose 业务失败（NXDOMAIN/SERVFAIL）与网络失败（Timeout/NetworkError）均结构化 200，仅入参非法 400；查询目标 env IPAM_DNS_DIAG_ADDR 默认 unbound:53。前端安全页占位换真表单（8 类型下拉+回车触发）+rcode Tag+答案表（TTL 空显示 -）。
- **验证结果**：dns 包单测 5 组全绿（含 v6 压缩/展开等价回归）；lint 0；typecheck 0 错；零外链 PASS；容器 e2e——A 查 example.com → 200 NOERROR + 2 条真实递归答案（ttl 300，rtt 1313ms）；PTR 查 8.8.8.8 → 200 Timeout 结构化（上游反查不可达时仍返回形态完整）。
- **遗留**：view 选择（按 §4.4 多视图来源区分）P2；DNSSEC 诊断 P2。
