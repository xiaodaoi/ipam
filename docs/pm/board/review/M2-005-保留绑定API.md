# [M2-005] 保留与静态绑定 API

| ID | M2-005 | 状态 | review | 来源 | §13.4 保留与绑定、FR-A-04 |
|---|---|---|---|---|---|
| 负责 | opencode(backend) | 创建 | 2026-08-26 |

## 目标
两类对象：冻结保留（不下发）与静态绑定（MAC↔IP host reservation）；批量 CSV 导入；与 Kea reservation-add/del 联动。

## 验收标准
- [ ] 冻结地址进入 excluded 表达；静态绑定走 host 后端
- [ ] CSV 批量导入事务性（部分失败全回滚+明细报告）

## DoD
单测+集成 / lint / FR-A-04 同步 / spec 先行 / commit [M2-005]

## 实施记录

### 2026-08-26 · 会话1
- **做了**：spec 先行——bulkReservations（混合 reserve/bind 批量，200 结果含逐行明细 / 503 KEA_DOWN）；LedgerService.BulkReservations 两阶段（预检零写入→应用→应用期失败尽力回滚已应用项）；ReservationRepo 增 Delete（回滚用）；handler 接线（复用 LedgerHandler）。
- **验证**：3 新单测全绿（全成功/占用行整体回滚含第1行/非法 MAC）；全仓 5 包 lint 0 issues。
- **踩坑留痕**：spec 内联数组结构在 codegen 中生成匿名 struct（非命名类型），handler 需同构匿名 struct 构造；两阶段预检保证"全或全不"由零写入+回滚双保险实现。
- **遗留**：CSV 文件上传接口（当前为 JSON 数组等价语义；文件解析放前端/网关层）；Kea 侧 reserve 实际 excluded 段回写待闸⑥端到端。
