# [M2-025] 封禁条目前端管理区

| 字段 | 内容 |
|---|---|
| ID | M2-025 |
| 状态 | review |
| 来源 | M2-024 遗留：条目 spec/后端已完整（list/add/delete 全有且 e2e 验证），但 blocklist 前端页无条目区——查/加/删条目无入口 |
| 负责 | opencode(frontend) |
| 创建 | 2026-08-29 |
| 更新 | 2026-08-29 |

## 方案

blocklist 页加条目 Card：
1. ipam.ts：listBlocklistEntries/addBlocklistEntry 函数 + BlocklistEntryRow 类型（deleteBlocklistEntry 已有）
2. 列表行 op 加「条目」按钮 → 选中列表（selId/selName）→ 展开条目 Card
3. 条目 Card：添加表单（pattern/triggerType/action）+ Table（pattern/triggerType/action/category + 行删除按钮）

## 验收标准（可测）

- [x] typecheck/零外链 PASS（API 层 e2e 已由 M2-024 全覆盖）
- [x] 容器重建 + entries API 回归（add 201 → list 200 → delete 204 全通——页三入口数据源）
- [x] 文档三件套 + commit [M2-025]

## 实施记录（追加式，勿删旧条目）

### 2026-08-29 · 会话1
- **做了**：ipam.ts（BlocklistEntryRow 类型 + listBlocklistEntries/addBlocklistEntry）；blocklist 页条目 Card——列表行「条目」按钮（openEntries(record as Blocklist)）→ 条目 Card（添加表单 + entryCols Table + 行删除按钮 + 关闭）；eForm 枚举类型收窄（'qname'|'response_ip' / 'nxdomain'|'drop'|'tcp_only'|'redirect'）。
- **验证结果**：typecheck 0 + build:ipam 6.51s ✓ + sync ✓ + 零外链 PASS；容器重建 + entries API 回归完整——add 201 → list 200（1 条 *.m2-025.test/qname/nxdomain）→ delete entry 204 → delete list 204，页三入口数据源全通（API 层决定性覆盖在 M2-024）。
- **踩坑留痕**：schema 枚举字面量联合的 TS2322（eForm 声明 string → 收窄为枚举联合）；Table slot record 类型断言（M2-021 同款）。
- **遗留**：列表级 PATCH 编辑（rename 影响 rpz zone 命名链路）P2；builtin 拒删 409 分支未 e2e 覆盖（DB 无 builtin 列表）P2。
