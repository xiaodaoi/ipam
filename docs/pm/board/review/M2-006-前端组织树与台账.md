# [M2-006] 前端页面：组织树侧栏 + 地址台账矩阵

| ID | M2-006 | 状态 | review | 来源 | §13.4、F-R1 前置 |
|---|---|---|---|---|---|
| 负责 | opencode(frontend) | 创建 | 2026-08-26 |

## 目标
web-ipam 新增「DHCP 服务」一级菜单与二级页骨架：左侧组织树组件（复用）、台账六态着色矩阵、租约详情抽屉；数据全部走生成的 TS 客户端。

## 验收标准
- [ ] 权限码 dns./dhcp. 命名空间接入动态菜单
- [ ] 六态颜色与 §13.4 规范一致（快照测试）

## DoD
构建+typecheck 绿 / 组件测试 / §13.4 一致 / N/A / commit [M2-006]

## 实施记录

### 2026-08-26 · 会话1
- **做了**：TS schema 类型生成管线（openapi-typescript→src/api/schema.d.ts，gen 脚本已纳入再生成流程）；类型化 API 客户端 src/api/ipam.ts（§13.2：统一 fetch 包装，禁散装）；DHCP 服务一级菜单路由（routes/modules/dhcp.ts，自动 glob 加载）；地址台账页（组织树侧栏 antdv Tree 适配+六态着色 Tag 矩阵+保留/静态绑定行操作）；中英双语 locale。
- **验证**：typecheck 绿（3 轮类型修正：bodyCell record 强转/Tree DataNode 适配/Key 类型）；build:ipam 11 tasks 成功；sync+零外链断言 PASS；Go embed 构建 OK。
- **踩坑留痕**：vue-tsc 对 antdv 泛型严格——Tree 需预转 key/title DataNode、bodyCell 的 record 是 Record<string,any> 需 as LedgerRow；禁用 JSX（项目无 tsx 配置）改 bodyCell 插槽。
- **遗留**：动态菜单/RBAC 权限码（M5）；台账分页控件与租约详情抽屉（P1 完善）；演示数据种子（M3 仪表盘卡）。
