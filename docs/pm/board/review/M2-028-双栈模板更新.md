# [M2-028] 双栈模板更新（update 端点 + 编辑模式）

| 字段 | 内容 |
|---|---|
| ID | M2-028 |
| 状态 | review |
| 来源 | CRUD 完整性扫描遗留：编辑闭环最后一块——dualstack templates 只有 list/create/delete，无 update；三层都缺 |
| 负责 | opencode(backend+frontend) |
| 创建 | 2026-08-29 |
| 更新 | 2026-08-29 |

## 方案

1. spec：Item 加 patch（updateDualstackTemplate 全量更新）+ DualstackTemplateUpdate schema + gen
2. 后端：Store 接口 + ErrTemplateNotFound + Mem/Pg Update + handler UpdateDualstackTemplate（CIDR 校验/errors.Is 404/200）
3. 前端：ipam.ts updateDualstackTemplate + dualstack 页编辑模式（editingId/edit()/cancelEdit()/add 分支/动态文案/op 编辑按钮）

## 验收标准（可测）

- [x] typecheck/全链绿 + 零外链 PASS
- [x] e2e：建模板 → PATCH 改 ipv6Prefix/expr/graceHours → 回读一致 → 清理
- [x] 文档三件套 + commit [M2-028]

## 实施记录（追加式，勿删旧条目）

### 2026-08-29 · 会话1
- **做了**：spec patch+schema+gen；store.go（errors import/ErrTemplateNotFound/接口 Update/Mem 遍历替换/Pg UPDATE...WHERE RowsAffected 判 404）；handler.go（fromGenUpdate/UpdateDualstackTemplate）；前端 ipam.ts updateDualstackTemplate + dualstack 页编辑模式（form 显式枚举类型收窄）。
- **验证结果**：go build/test/lint 全绿 + typecheck 0 + 零外链 PASS；e2e——建模板 85db3303 → PATCH 200 → 回读 ipv6Prefix=2406:180::/64 expr={v4.hextet5} graceHours=48 全一致 → DELETE 204。
- **踩坑留痕**：① 多行函数定义（`=> \n req(...)`）中间行级插入切断函数体（TS1005）——插入前确认目标是完整语句；② form 对象字面量的 encoding 推断为 string → TS2345——显式泛型收窄为枚举联合（M2-025 同款）。
- **遗留**：无——**编辑闭环 100%**（子网/上游/转发/选项类/用户/区域/名单/模板全站 CRUD 齐）。
