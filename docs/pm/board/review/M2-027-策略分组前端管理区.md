# [M2-027] 策略分组前端管理区

| 字段 | 内容 |
|---|---|
| ID | M2-027 |
| 状态 | review |
| 来源 | M2-026 扫描遗留：spec/后端已有 policy-groups 三端点（GET 列表/POST 创建/POST {groupId}/compile——RPZ 编译），前端完全未接（blocklist 页尾预告「策略分组……属后续批次」） |
| 负责 | opencode(frontend) |
| 创建 | 2026-08-29 |
| 更新 | 2026-08-29 |

## 方案

blocklist 页加策略分组 Card（M2-025 同款模式）：
1. ipam.ts：listPolicyGroups/createPolicyGroup/compilePolicyGroup + PolicyGroupRow 类型
2. 策略分组 Card：创建表单（name/viewName/cidrs 逗号分隔/listIds 多选=现有名单）+ Table（名单数列 + 行「编译」按钮）+ pgResult pre 展示编译结果
3. onMounted 挂载加载

## 验收标准（可测）

- [x] typecheck/零外链 PASS
- [x] 容器重建 + policy-groups API 回归（create → list → compile 回读真实产出）
- [x] 文档三件套 + commit [M2-027]

## 实施记录（追加式，勿删旧条目）

### 2026-08-29 · 会话1
- **做了**：ipam.ts（PolicyGroupRow 类型 + 三函数）；blocklist 页策略分组 Card——创建表单（name/viewName/cidrs 逗号分隔 split/listIds 多选 Select 关联现有名单）+ pgCols Table（名单数列 + 编译按钮）+ pgResult pre 展示编译结果（zone/entries/path/reloadCommand）；onMounted 挂载加载；说明文案更新（策略分组已实现）。
- **验证结果**：typecheck 0 + build:ipam FULL TURBO ✓ + sync ✓ + 零外链 PASS；容器重建 + policy-groups API 全链回归——加条目 201 → 建分组 4832aa61 → list 回读一致（1 组/recursor/1 名单）→ **compile 真实产出 RPZ zone**（/var/lib/ipam/rpz/recursor.zone，entries=1，reloadCommand=unbound-control auth_zone_reload recursor.rpz）；psql 清理回归残留。
- **踩坑留痕**：M2-026 的 import 替换落点与字母序不符（createBlocklist 在 listBlocklistEntries 后）——**按文件现场形态锚定，不按推演**。
- **遗留**：无——**blocklists 域全闭环**（名单查/建/删/同步 + 条目查/加/删 + 策略分组查/建/编译，前端入口全通）；提示页定制 P2。
