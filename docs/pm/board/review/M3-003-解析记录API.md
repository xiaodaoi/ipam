# [M3-003] 解析记录 API（auth-zone/local-zone）

| ID | M3-003 | 状态 | review | 来源 | FR-B-03~05、§13.4 解析记录 |
|---|---|---|---|---|---|
| 负责 | opencode(backend) | 创建 | 2026-08-26 |

## 目标
zone 与记录 CRUD（A/AAAA/CNAME/PTR 正向反向页签）；静态 zonefile 导出+checkconf+auth_zone_reload；联动记录只读视图。

## 验收标准
- [ ] 记录变更单区刷新不动整进程（auth_zone_reload）
- [ ] 联动视图=coherence 绑定生成记录（只读）

## DoD
单测 / lint / §2.3 同步 / spec 先行 / commit [M3-003]

## 实施记录

### 2026-08-26 · 会话1
- **做了**：spec 先行——dns zones/records/linked/export 六端点（含 text/plain zonefile 响应示例）；迁移 0006（dns_zone/dns_record，UNIQUE(zone,name,type)）；ValidateRecord 类型语法校验（A/AAAA/CNAME/PTR）；ExportZonefile（$ORIGIN/$TTL+记录行，禁用行跳过）；ZoneService（创建记录→auth_zone_reload 单区刷新；非法记录不触发）；engine/unbound AuthZoneReload；Pg/Mem 双仓储；ZoneHandler 接线（linked 注入来自 ipam 绑定→A/AAAA 联动只读视图，LedgerBinding 补 IPv6 字段）。
- **验证**：4 新单测全绿（类型校验矩阵/zonefile 格式/刷新触发/非法不触发）；全仓 7 包 lint 0 issues。
- **踩坑留痕**：同路径双 get 操作→独立 /linked 子路径；联动视图用注入式避免包循环；PG ON CONFLICT DO NOTHING 以 ErrNoRows 映射 RECORD_NAME_DUP。
- **遗留**：zonefile 导出→checkconf→auth_zone_reload 的完整链路容器实测在 M3-006；PTR 联动记录（reverse zone）P1。
