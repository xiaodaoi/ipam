# [M2-017] 保留与绑定页

| 字段 | 内容 |
|---|---|
| ID | M2-017 |
| 状态 | review |
| 来源 | 架构文档 §561（DHCP 菜单 4/6）：保留地址列表（冻结不下发）/ 静态绑定列表（MAC↔IP）；M2-005 API |
| 负责 | opencode(frontend) |
| 创建 | 2026-08-28 |
| 更新 | 2026-08-28 |

## 目标

DHCP 菜单第 4 项落地：批量创建保留/静态绑定（bulkReservations，CSV 语义事务性）+ 保留列表 + 静态绑定列表（Ledger state 过滤复用，零后端改动）。

## 验收标准（可测）

- [ ] ipam.ts 补 bulkReservations client；页面三卡（批量创建/保留列表/绑定列表）
- [ ] DHCP 菜单加「保留与绑定」child；typecheck 0/零外链 PASS
- [ ] 容器 e2e：bulk 创建 2 保留+1 绑定 → 200 逐行明细；state=reserved/bound 回读一致
- [ ] commit [M2-017]

## 涉及模块

- `web/apps/web-ipam/src/{api/ipam.ts,views/dhcp/reservations/index.vue}` + dhcp 路由/语言包
- 后端零改动（bulkReservations + listLedger state 过滤均已就绪）

## 实施记录（追加式，勿删旧条目）

### 2026-08-28 · 会话1
- **做了**：bulkReservations client + 三卡页（批量创建动态行 kind/address/mac/reason、保留列表、静态绑定列表，后端零改动——bulk API + ledger state 过滤全复用）+ DHCP 菜单「保留与绑定」child。
- **诊断战果（超出卡片的既有缺陷修复）**：页面联调发现台账全量 0 行——逐层实证（ledgerSrc 数量日志→QueryLedger 全文→subnetCols→address_pool 表）定位 **PgSubnetRepo List/Get 从不回填 Pools**：Create/Update 写 address_pool 但读路径漏掉 → 台账逐池枚举 0 行 + **kea 下发 subnet4 空池**（连带既有缺陷）→ loadPools（List/Get 共用回填，ANY 查询）修复。
- **验证结果**：全链测试绿 + lint 0 + typecheck 0 + 零外链 PASS；容器 e2e——pools 回读有值、台账 100 行（available 97/reserved 2/static 1）、**state=reserved 精确返回 4 条**（双子网）；bulk 事务性验证（bind 失败 → ok:false 整体回滚）。
- **附带发现**：① bulk 回滚对 bind 缺口——kea 下发失败时 bind 的 Upsert 残留（第一次 e2e 的 .40 残留实证）；② Debian kea 2.2 无 host_cmds 库，reservation-add 命令不可用；③ M2-016 非法 test 曾打 kea FATAL 污染子网创建（pools 回滚丢失）——环境性事件。
- **遗留 → 立卡**：**M3-007 bind 配置式下发**（subnet reservations 投影 + bulk 回滚缺口 + 废弃 reservation-add 命令路径）P1；DHCPv6 PD 前缀委派 P2。
