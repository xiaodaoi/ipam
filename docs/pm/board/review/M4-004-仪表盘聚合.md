# [M4-004] 仪表盘聚合 API

| ID | M4-004 | 状态 | review | 来源 | F-01、§13.4 仪表盘 |
|---|---|---|---|---|---|
| 负责 | opencode(backend) | 创建 | 2026-08-26 |

## 目标
单端点聚合仪表盘数据：今日在线 IP 数与趋势、新增/离线终端、服务健康灯、DNS QPS/命中率/拦截量、池利用率 TopN、联动成功率。

## 验收标准
- [x] 单请求 ≤1s（CH uniqExact 小窗聚合+每池一次计数，池数个位量级；无预聚合依赖）
- [x] 前端仪表盘页消费展示（路由 /dashboard/overview，30s 自动刷新）

## DoD
单测 / lint / §13.4 同步（口径入 spec description 与卡）/ spec 先行 / commit [M4-004]

## 实施记录

### 2026-08-27 · 会话1
- **spec 先行**：`GET /dashboard`（getDashboardOverview，tag dashboard）+ DashboardOverview/PoolUtilization/HealthLight schema；不可测指标返回 null 而非伪造值（hitRatePct 恒 null、poolTop 默认 5）；闸① 0 errors。
- **logquery.Store 扩三个聚合口径**：DistinctClientIP（uniqExact + 可选 AddrRange 闭区间——池利用率口径）、HourlyActive（逐小时 distinct client_ip）、MacActivity（MAC min/max 窗口）；ChStore/MemStore 双实现，Mem 复用 matchIP/matchScope/mappedV6 同语义。
- **dashboard 模块**（internal/module/dashboard）：Service 多源编排（终端流量=近7天 MAC 首见/末见推导新增·离线；DNS=qps5m 全 dns 口径+今日 blocked 计数；池利用率=每池区间 distinct/capacity，big.Int 容量计算 int64 饱和；联动成功率=active/(active+conflict)）；Lights 探针集 nil=unknown；now 可注入供测试。
- **main.go 装配**：pgLight(pool.Ping)/chLight(仅配置 IPAM_CH_ADDR 时点亮，PoC=unknown)/tcpLight(kea @ IPAM_KEA_API)/unixLight(unbound ctl socket)，全部 800ms 超时；dashAPI 命名包装规避嵌入名冲突（第三处同款坑）。
- **前端**：api/ipam.ts 类型化 getDashboard；routes/modules/dashboard.ts 替换 vben 示例为 IpamOverview；views/dashboard/ipam/index.vue 卡片组（活跃数+24h CSS 柱状趋势+健康 Badge 行+DNS 三指标+池利用率 Table），30s 轮询自动刷新；zh/en locale。
- **验证**：dashboard 3 组单测绿（含坏池跳过/区间外与昨日剔除/补零 24 槽）；全仓 go test 绿；golangci-lint 0 issues；web typecheck/build/零外链断言 PASS；闸② 覆盖新路由。
- **遗留**：① hitRatePct 与组织级 DNS 口径同受 unbound 源 IP 提取（M4-001 遗留②）制约；② keepalived/VRRP 健康灯未接入（无探测路径，unknown 兜底）。