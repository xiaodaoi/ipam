# [M2-003] 地址台账 API（六态聚合视图）

| ID | M2-003 | 状态 | review | 来源 | §13.4 地址台账 |
|---|---|---|---|---|---|
| 负责 | opencode(backend) | 创建 | 2026-08-26 |

## 目标
台账查询：按组织节点过滤的 v4/v6 地址矩阵（六态判定：在线=租约活跃近似）、行内租约详情、转保留/转静态绑定操作端点。

## 验收标准
- [ ] 六态判定单测全覆盖；分页游标规范
- [ ] 操作端点联动 Kea reservation 与 excluded 段

## DoD
单测 / lint / §13.4 六态表同步 / spec 先行 / commit [M2-003]

## 实施记录

### 2026-08-26 · 会话1
- **做了**：spec 先行——ledger 三端点（listLedger 六态矩阵+游标分页 / reserveAddress / bindStatic，含 ADDR_OCCUPIED 409）；ledger.go 六态判定（§13.4 颜色规范全矩阵）+ QueryLedger（v4 逐地址枚举含池内、v6 子网级汇总、游标切片、状态过滤）；LedgerService（Reserve/BindStatic：占用检查以预留仓储为准→写预留→Kea 下发）；PgReservationRepo/MemReservationRepo；KeaDeployer 扩展 ReserveAddress/BindStatic（CtrlAgent reservation-add 实现）；LedgerHandler+main 装配（绑定源 PG 读 coherence_binding）。
- **验证**：ledger 新 4 单测全绿（六态矩阵/游标分页续读/占用拒绝/绑定幂等占用）；全仓 5 包 build/vet/lint 0 issues。
- **踩坑留痕**：LedgerBinding 需在 ipam 域自建类型隔离 coherence 耦合；占用判定必须查仓储而非 source 快照（否则幂等性失效）；spec 内联 Problem examples 的 content 级缩进反复踩坑已形成模板认知。
- **遗留**：六态中"在线"为租约活跃近似（无精确探测，符合 §13.4 决策）；asset 资产表关联进台账（owner 字段已预留，M2-004 接线）；闸⑥ 台账端到端补验。
