# [M2-002] DHCPv4/v6 子网与地址池 API + Kea 下发

| ID | M2-002 | 状态 | review | 来源 | FR-C、§2.2 |
|---|---|---|---|---|---|
| 负责 | opencode(backend) | 创建 | 2026-08-26 |

## 目标
subnet/pool CRUD（v4/v6/PD，挂 org_id）→ 写 PG → engine/kea config-set 三步走（staged 校验失败回滚）；dry_run 预检支持。

## 验收标准
- [ ] compose 环境实测：创建子网后 kea-dhcp4 config-set 成功且 curl ctrl-agent 确认生效
- [ ] Idempotency-Key 幂等重放不重复下发

## DoD
集成测试 / lint / §2.2 同步 / spec 先行 / commit [M2-002]

## 实施记录

### 2026-08-26 · 会话1
- **做了**：spec 先行——subnets 四端点（列表过滤/创建含 dryRun/更新/删除，全部含双 example 与 503 KEA_DOWN 语义）；迁移 0003（subnet/address_pool 表，org FK）；ipam 域——SubnetService（org 校验/CIDR-族校验/创建时引擎先发后库、失败不落库；更新快照回滚；删除引擎先摘除）；PgSubnetRepo+OrgRepo(pgx) 与 Mem 双实现按 IPAM_DB_DSN 装配；engine/kea——BuildConfig（模板+subnet4 段组装，v6 过滤）与 CtrlAgent（config-set 数组响应解析/result≠0 报错/dryRun 不联网）；SubnetHandler 接线入组合 struct。
- **验证**：ipam+kea 新增 10 单测全绿（dryRun 不落库/引擎失败回滚/数组响应/HTTP 错误/族校验）；全仓 build/vet/golangci-lint 0 issues。
- **踩坑留痕**：Kea ctrl-agent 返回 JSON 数组（每 service 一项）非单对象；spec 内联 Problem 的 examples 必须 content 级；paths 内 description 含半角冒号+空格需引号或改词；测试函数名禁 '+'。
- **遗留**：compose 真实 config-set 端到端（闸⑥ 扩展为创建子网→kea 生效）随前端页卡 M2-006 后统一补验；租约引用删除保护（SUBNET_IN_USE）在租约表落地后启用。
