# [M2-011] DNS 缓存与性能 + 安全诊断页

| 字段 | 内容 |
|---|---|
| ID | M2-011 |
| 状态 | review |
| 来源 | §13.4 DNS 服务 3/6（缓存与性能）、6（安全与诊断部分）；M3-005 settings API |
| 负责 | opencode(frontend) |
| 创建 | 2026-08-28 |
| 更新 | 2026-08-28 |

## 目标
缓存与性能页：TTL 参数表单（min/max/serve-expired）+ 手动清空缓存 + 每域名 TTL 覆盖列表。
安全与诊断页：RRL 开关与限速值 + DNSSEC 开关（占位提示）+ 解析测试台（dig 式，P1 占位）。

## 验收标准
- [ ] settings 表单加载/保存（保存调 PUT 语义端点）
- [ ] flush 按钮调用清空缓存并提示结果
- [ ] TTL 覆盖列表增删
- [ ] typecheck/build/零外链 PASS

## 涉及模块
- web/apps/web-ipam/src/{api,views/dns/cache,views/dns/security}

## DoD 自检
- [ ] typecheck 通过
- [ ] lint 通过
- [ ] 进度日志
- [ ] API 已存在（M3-005）
- [ ] commit 带 [M2-011]

## 实施记录（追加式，勿删旧条目）

### 2026-08-28 · 会话1
- **做了**：缓存与性能页（TTL min/max+serve-expired 表单保存、flush 全部/指定 zone、每域名 TTL 覆盖增改列表）；安全与诊断页（RRL 开关+限速值、DNSSEC P2 禁用占位、解析测试台指引占位）；DNS 菜单扩至六子页齐。
- **验证结果**：typecheck/build/零外链 PASS；容器部署两路由 200；GET /dns/settings 实测返回默认参数。
- **遗留**：解析测试台真实现（view 选择+来源 dig）P1；DNSSEC P2。
