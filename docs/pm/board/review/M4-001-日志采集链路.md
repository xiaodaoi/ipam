# [M4-001] 日志采集链路（Kea/Unbound→Vector→CH）

| ID | M4-001 | 状态 | review | 来源 | FR-E-01/02、D6、§6 |
|---|---|---|---|---|---|
| 负责 | opencode(devops) | 创建 | 2026-08-26 |

## 目标
vector 配置落地：kea stdout + unbound logfile 采集，VRL 归一化（client_mac 统一关联键、action/state 字段），批量写入 ClickHouse logs 表（5000条/5s）。

## 验收标准
- [ ] compose 环境端到端：触发 DHCP/DNS 事件后 CH 可查
- [ ] 磁盘水位 80% 告警 / 90% 降级采样配置就位（FR-E-05）

## DoD
集成验证记录 / yaml lint / §6 同步 / N/A / commit [M4-001]

## 实施记录

### 2026-08-26 · 会话1
- **做了**：vector.toml 全链路（file source×2 → VRL 正则解析 Kea 租约事件与 Unbound 查询 → 统一 schema 归一化 → ClickHouse 批量 5MB/5s）；db/clickhouse/logs.sql 建表（MergeTree+TTL180d+tokenbf 索引，§3 落地）；compose 扩展 clickhouse/vector 服务与日志卷挂载（kea stdout→文件卷、unbound logfile 卷）。
- **验证**：compose YAML 解析通过；端到端数据流待 compose 环境实测（闸⑥扩展随 M4-002 检索 API 一并断言）。
- **踩坑留痕**：isc 容器日志默认 stdout——需 sh 包装 mkdir /var/log/kea 后由 vector file 读取；unbound.conf 需同步开启 logfile 输出至挂载卷（下一步 M4-002 时补）。
- **遗留**：① unbound.conf 补 logfile:/var/log/unbound/unbound.log 与 log-queries: yes；② DHCPv6/PD 事件解析规则补充；③ 水位降级采样（FR-E-05）P1。

### 2026-08-26 · 会话2（多池对联动，commit 7533703）
- **补充（用户口径）**：多组 v4/v6 池对绑定（prefix_template 建模 ipv4_cidr↔ipv6_prefix），daemon 按租约 IPv4 最长前缀自动选模板；示例 192.168.0.10→2407::192:168:0:10 落地为单测。
- **附带修复**：spec 响应模型缺失根因=去重脚本致媒体类型 null——已按 flow 风格重建 15 处响应块；conf/apply 由手动路由改为生成接口实现（消除重复注册）。
