# [M2-004] 资产登记 API（asset，MAC 身份键）

| ID | M2-004 | 状态 | review | 来源 | §13.4 资产登记 |
|---|---|---|---|---|---|
| 负责 | opencode(backend) | 创建 | 2026-08-26 |

## 目标
asset CRUD：以 MAC 为键登记使用人/部门/备注/标签；换址不丢；与日志中心关联查询预留 org 维度。

## 验收标准
- [ ] upsert 幂等；org_id 外校验存在性
- [ ] 台账页签数据源即本 API

## DoD
单测 / lint / N/A 文档 / spec 先行 / commit [M2-004]

## 实施记录

### 2026-08-26 · 会话1
- **做了**：spec 先行——asset 三端点（listAssets 过滤/upsertAsset 幂等/deleteAsset）；AssetService（MAC 归一化校验复用 coherence.NormalizeMAC、org 校验、幂等 upsert）；Pg/Mem 双仓储（ILIKE 关键字过滤、ON CONFLICT upsert、tags 数组）；AssetHandler 接线；main 台账资产源接通（DSN 时读 PG asset 回填 ledger Assets→owner 字段）。
- **验证**：asset 3 新单测全绿（归一化+幂等/非法 MAC/过滤+删除）；全仓 5 包 lint 0 issues。
- **踩坑留痕**：ledger.go 曾自建迷你 Asset 类型致重复声明——统一收敛 asset.go 单一类型；NormalizeMAC 从 coherence 域复用避免复制逻辑。
- **遗留**：CSV 批量导入在 M2-005 承接。
