# [M2-020] 子网编辑功能（网关/DNS 修改闭环）

| 字段 | 内容 |
|---|---|
| ID | M2-020 |
| 状态 | review |
| 来源 | M2-019 补完：网关/DNS 后台可配的编辑闭环（现改配置需删重建）；PATCH handler 已就绪 |
| 负责 | opencode(frontend) |
| 创建 | 2026-08-28 |
| 更新 | 2026-08-28 |

## 方案

子网页复用创建表单为编辑模式：行「编辑」按钮 → 表单预填 → PATCH 提交（updateSubnet）→ notify 全量重下发（M3-007 融合 ✓ 网关/DNS 修改经 config-set 实际生效）。

## 验收标准（可测）

- [x] ipam.ts updateSubnet；行编辑按钮 + 表单预填（name/cidr/gateway/dns/pools）+ family 编辑时禁用
- [x] typecheck/零外链 PASS
- [x] e2e：PATCH 改网关 → kea config-get 实收新网关（决定性）
- [x] commit [M2-020]

## 实施记录（追加式，勿删旧条目）

### 2026-08-28 · 会话1
- **做了**：ipam.ts updateSubnet（patch helper 复用）；子网页编辑模式——editingId 状态 + edit() 行预填（name/cidr/gateway/dns/pool 首行/kind/len）+ add() PATCH/POST 分支 + 提交按钮动态文案 + family 编辑禁用 + 新建按钮清 editingId + 操作列编辑按钮。
- **验证结果**：typecheck 0 + 零外链 PASS + 全链测试绿 + lint 0；容器 e2e 决定性通过——PATCH 编辑网关（10.99.3.1 → 10.99.3.254）→ 200 → **kea dhcp4 config-get 实收 routers: 10.99.3.254**（编辑→DB→kea 实收闭环）→ 回读一致。
- **踩坑留痕**：python heredoc 的 old 缩进与文件实际（tab/空格）差异导致多处静默未命中——改用 Edit 工具逐个精确锚定（失败显式报错）。
- **遗留**：无（编辑闭环完成）。
