# [M2-039] 系统设置二级菜单（Web 页面设置）

| 字段 | 内容 |
|---|---|
| ID | M2-039 |
| 状态 | review |
| 来源 | 用户清单（五-5）：增加系统设置二级菜单，包含 web 页面设置，修改图标/名称、浏览器页签 LOGO/名称、上传 logo、服务器 IP 和端口 |
| 负责 | opencode(full-stack) |
| 创建 | 2026-08-31 |
| 更新 | 2026-08-31 |

## 方案（批 1 后端 / 批 2 前端 / 批 3 收尾）

- **批 1（后端）**：GET/PUT /system/webui-settings + webui_settings 单行表（迁移 0017）+ WebuiHandler（serverIp/serverPort 从请求 Host 只读解析——修改走部署配置，Web 改部署配置是反模式）。
- **批 2（前端）**：system 下「系统设置」二级菜单页（siteName/faviconUrl/logoUrl 输入 + serverIp/serverPort 只读展示）+ 动态应用（document.title + favicon link）。
- **批 3（收尾）**：e2e + 文档 + 收口。

## 验收标准（可测）

- [x] 批 1：go build/test 全绿 + lint 0 issues
- [x] 批 1 e2e：GET/PUT 200 + 持久化复核 + PUT 缺 siteName 400
- [x] 批 2：前端设置页 + 动态应用 title/favicon + 验证链绿
- [x] 文档三件套 + commit [M2-039]

## 实施记录（追加式，勿删旧条目）

### 2026-08-31 · 会话1（批 1：后端 webui-settings）
- **做了**：spec 三处（paths/webui.yaml + schemas/webui.yaml + openapi.yaml 注册 65:66）+ gen（GetWebuiSettings 6 处）+ 迁移 0017（webui_settings 单行表）+ webui_handler.go（Mem/Pg repo + GET/PUT + Host 只读解析）+ main/handler_test 装配。
- **验证结果**：BUILD_OK + go test 无失败 + lint 0 issues + 容器重建（Built=1）+ e2e 四步全过（GET 初始 200 / PUT 200 / GET 持久化 200 / PUT 缺 siteName 400）。
- **踩坑留痕**：① 批 1 首次实施全部丢失（spec/gen/迁移/handler/装配 0 残留）——**每步同批验证（ls/grep）**，重建后全部实锤；② Save 的 ON CONFLICT 用 $4 引用但只传 3 参数（mismatched param and argument count → 500）——**upsert 参数复用必须用 EXCLUDED**。
- **遗留**：无——M2-039 全部完成（批 3 e2e 并入批 2 验证链）。

### 2026-08-31 · 会话1（批 2：前端系统设置页 + 路由 + 动态应用）
- **做了**：settings/index.vue 设置页（siteName/faviconUrl/logoUrl 输入 + serverIp/serverPort 只读展示 + apply 动态应用 document.title/favicon link + save PUT + load GET）+ system.ts「系统设置」路由（/system/settings + lucide:settings + authority system:read）。
- **验证结果**：typecheck 0 + build ✓ 7.60s + sync 4.3M + 零外链 PASS + 容器重建（Built=1）+ webui-settings API 冒烟 200（siteName 持久化复核）。
- **v1 决策**：logo 上传以 URL 输入实现（文件上传 v2）；服务器 IP/端口只读展示（Web 改部署配置是反模式——改完失联）。
- **遗留**：无——M2-039 全部完成。
