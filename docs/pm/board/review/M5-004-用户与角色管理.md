# [M5-004] 用户与角色管理

| 字段 | 内容 |
|---|---|
| ID | M5-004 |
| 状态 | review |
| 来源 | §13.4 系统管理；M5-001 遗留（PoC 直通→真实账号）；M5-003 RBAC 消费方落地 |
| 负责 | opencode(backend/frontend) |
| 创建 | 2026-08-28 |
| 更新 | 2026-08-28 |

## 目标

登录从 PoC 硬编码单账号升级为 users 表真实账号（bcrypt）：用户 CRUD + 角色指派（admin 写/user 只读，对齐既有 RBAC 语义）+ 启停；前端系统管理菜单新增用户页。

## 验收标准（可测）

- [ ] 迁移 0010 users 表；启动引导：无用户时自动建 admin（口令沿用 IPAM_POC_PASSWORD/admin123 语义，升级兼容）
- [ ] spec /users CRUD（闸① 0 errors）+ gen；tag=platform
- [ ] 登录走 bcrypt 校验 + 按 DB 角色签发 JWT claims；禁用账号登录 401
- [ ] 自保护：不能删除/停用自己、不能改自己角色；删除最后一名启用 admin 拒绝（单测覆盖）
- [ ] 前端系统管理菜单 + 用户页（列表/新建/重置密码/启停/角色）；typecheck/零外链 PASS
- [ ] 容器 e2e：新建 user 角色账号 → 登录成功 → 写操作 403（RBAC 真实消费）；admin 原口令仍可登录
- [ ] lint 0、commit [M5-004]

## 涉及模块

- `db/postgresql/migrations/0010_users.sql`
- `internal/module/platform/{user.go,user_handler.go,auth_handler.go,auth_poc.go}`
- `api/openapi/{paths/user.yaml,components/schemas/user.yaml,openapi.yaml}`
- `web/apps/web-ipam/src/{api/ipam.ts,router/routes/modules/system.ts,views/system/users/index.vue}`

## 实施记录（追加式，勿删旧条目）

### 2026-08-28 · 会话1
- **做了**：spec /users CRUD（tag 复用既有 platform——发现根 spec 早有该 tag，去重；闸① 0 errors）+ gen；迁移 0010 users 表；user.go Mem/Pg 双 Store + bcrypt（x/crypto 升 direct，LICENSES 登记）+ EnsureBootstrap（口令语义沿用 IPAM_POC_PASSWORD/admin123，升级兼容）；user_handler 自保护（不能删/停自己、不能改自己角色）+ 最后一名启用 admin 守卫；auth login 走 bcrypt + 按 DB 角色签发 claims；/auth/codes 返回角色；前端系统管理菜单追加用户页（创建/重置密码/启停/角色标签/自身行禁操作）。
- **验证结果**：平台包 6 组新单测全绿（引导幂等/CRUD/自保护/最后一名 admin/入参校验/禁用登录）；lint 0；typecheck 0；零外链 PASS；容器 e2e 8 项全过——admin 登录 ✓、op01 创建 201、op01 登录 ✓、读列表 200、写操作 403（RBAC 真实消费）、admin 自改角色 400、停用 200、禁用后登录 401。
- **踩坑留痕**：① 既有 PG 卷不会自动应用新迁移（initdb.d 仅首次初始化执行）→ 手动 psql 应用 0010；② TestLastAdminGuard 初版用 admin 自己令牌停自己，先撞自保护——修正为 admin2 令牌操作才能走到 LAST_ADMIN 路径；③ 覆盖 system.ts/page.json 前未查既有内容，短暂丢掉 SystemOrgs 路由与 orgs 语言键——已恢复（教训：追加前先读文件）。
- **遗留**：存量令牌无吊销（禁用账号的 JWT 至 24h 到期仍有效）P2；迁移 runner（既有卷自动应用增量）P1 建议立卡；角色细粒度 scope 按需。
