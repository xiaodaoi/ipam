# [M2-026] 前端 API 补齐（名单新建 + 区域删除）

| 字段 | 内容 |
|---|---|
| ID | M2-026 |
| 状态 | review |
| 来源 | CRUD 完整性扫描（spec paths × 前端 61 函数对照）发现两个前端缺口：① spec/后端有 createBlocklist（M2-024 e2e 用 curl 建的），前端无入口——blocklist 页可管条目却无法新建名单；② spec/后端有 deleteDnsZone（裸删 404 语义），records 页 zone 行无删除按钮 |
| 负责 | opencode(frontend) |
| 创建 | 2026-08-29 |
| 更新 | 2026-08-29 |

## 方案

1. ipam.ts：createBlocklist + deleteDnsZone 函数 + BlocklistCreateRow 类型
2. blocklist 页：列表 Table 前「新建名单」表单（name/kind Select/syncUrl 仅 feed 显示）
3. records 页：zone Select 旁删区按钮（window.confirm 防误删——裸删语义，zone 下 records 一并消失；删除后重置选中并 loadZones）

## 验收标准（可测）

- [x] typecheck/零外链 PASS
- [x] 容器重建 + deleteDnsZone API 回归（create → delete 204 → 回读消失）；createBlocklist 由 M2-024 e2e 覆盖（POST 201）
- [x] 文档三件套 + commit [M2-026]

## 实施记录（追加式，勿删旧条目）

### 2026-08-29 · 会话1
- **做了**：ipam.ts（BlocklistCreateRow 类型 + createBlocklist/deleteDnsZone）；blocklist 页新建名单表单（cForm + createList + feed 条件 syncUrl 输入）；records 页 zone Select 旁删区按钮（removeZone——confirm + zoneId 重置 + loadZones 自动回选第一个）。
- **验证结果**：typecheck 0 + build:ipam ✓（FULL TURBO 缓存命中确认成功）+ sync ✓ + 零外链 PASS；容器重建 + deleteDnsZone API 回归——create zone 6f2cd408 → delete 204 → 回读已消失（GET 200 真验证）；createBlocklist API 由 M2-024 e2e 覆盖（201）。
- **踩坑留痕**：① 多行 HTML 标签中间插行破坏标签结构——Button 插进未闭合的 Select 开始标签（97-99 行），vue-tsc 通过但 vite build 报 `Invalid end tag`（records/index.vue:98）——**插入前必须确认标签闭合位置**；② python 中途 assert 失败时已写段与未写段状态不一致——**分段独立落盘**；③ vue-tsc 对模板解析错误的输出非 "error TS" 前缀，grep 过滤会掩盖——build 错误需看完整输出。
- **遗留**：policy-groups 前端 P2（页尾已注明）；/logs/tail、/logs/export 前端 P2；dualstack update P2（删建可接受）。
