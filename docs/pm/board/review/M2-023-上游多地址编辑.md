# [M2-023] 上游多地址编辑

| 字段 | 内容 |
|---|---|
| ID | M2-023 |
| 状态 | review |
| 来源 | M2-021 遗留：上游表单 addrs 单值输入——编辑预填截断为第 1 个地址，提交也只回 1 条 |
| 负责 | opencode(frontend) |
| 创建 | 2026-08-29 |
| 更新 | 2026-08-29 |

## 方案

addrs 逗号分隔输入（Input 语义不变，无动态行组件）：
- edit 预填：`addrs.join(',')`（全量回填）
- 提交（POST/PATCH 共用路径）：`split(',').map(trim).filter(Boolean)`

## 验收标准（可测）

- [x] typecheck/零外链 PASS
- [x] e2e：编辑上游为多地址（2 条）→ 回读 items addrs 2 条
- [x] 文档三件套 + commit [M2-023]

## 实施记录（追加式，勿删旧条目）

### 2026-08-29 · 会话1
- **做了**：upstream/index.vue 三处——edit 预填 `addrs.join(',')`（修复多地址截断为第 1 个）、PATCH 分支与 POST 分支提交 `split(',').map((s) => s.trim()).filter(Boolean)`（逗号分隔多地址输入）。
- **验证结果**：typecheck 0 + build:ipam ✓ + sync ✓ + 零外链 PASS；e2e——PATCH 多地址 200 + 回读 2 条（223.5.5.5:53, 119.29.29.29:53）。
- **踩坑留痕**：无（M2-021 编辑模式直接复用）。
- **遗留**：封禁条目编辑（条目增删已有，删建模式可接受）P2；双栈页编辑（spec 无 update）P2。
