# [M3-005] 缓存与安全参数 API

| ID | M3-005 | 状态 | review | 来源 | FR-B-06/08/10、F-R2/R3、§13.4 |
|---|---|---|---|---|---|
| 负责 | opencode(backend) | 创建 | 2026-08-26 |

## 目标
缓存参数（min/max TTL、serve-expired、每域 TTL 覆盖）+ 安全参数（RRL、DNSSEC 开关、防放大）+ 清空缓存操作。

## 验收标准
- [ ] 参数变更→unbound.conf 模板重渲染→checkconf→reload
- [ ] flush 操作 unbound-control flush_zone

## DoD
单测 / lint / FR-B 同步 / spec 先行 / commit [M3-005]

## 实施记录

### 2026-08-26 · 会话1
- **做了**：spec 先行——settings 四端点（get/update 含 400 INVALID_CONF/ttl-overrides 查设/flush）；迁移 0008（dns_settings/dns_ttl_override）；SettingsService（持久化→渲染→checkconf→reload，校验失败不改运行态；Flush all/zone）；RenderSettingsBlock（cache-min/max-ttl、serve-expired、ratelimit+per-ip、val-permissive-mode、do-udp 映射）；engine/unbound CheckConf/Reload/FlushZone；Pg/Mem 双仓储；SettingsHandler 接线。
- **验证**：3 新单测全绿（渲染映射矩阵/更新持久化+reload 触发/清空命令）；全仓 7 包 lint 0 issues。
- **踩坑留痕**：fake 桩方法签名对齐接口（CheckConf 三参）反复漏改；Flush 空 zone 命令应为 flush 而非 flush_zone。
- **遗留**：checkconf 当前为占位（完整五源合成校验在 M3-006）；每域 TTL 覆盖进 unbound.conf（local-zone 级）容器实测 M3-006。
