# [M2-024] 封禁列表条目删除 + 列表删除

| 字段 | 内容 |
|---|---|
| ID | M2-024 |
| 状态 | review |
| 来源 | 盘点更正：M2-023 卡遗留「条目增删已有」判断有误——spec 实证 entries 只能 add（addBlocklistEntry）无 delete；list 级也只有 create+sync 无 delete。加错的域名无法移除，真实缺口 |
| 负责 | opencode(backend+frontend) |
| 创建 | 2026-08-29 |
| 更新 | 2026-08-29 |

## 方案

1. spec：BlocklistItem 加 `delete`（deleteBlocklist）；entries 路径加 `delete`（deleteBlocklistEntry，pattern query 参数——(list_id, pattern) 自然键）；gen
2. 后端：BlocklistRepo 接口 + errBlocklistNotFound + Mem/PG 双实现（DeleteList 级联删条目；DeleteEntry 按 (list_id, pattern)；RowsAffected==0 判 not found）+ handler 2 端点（DeleteBlocklist：404/409 builtin/204；DeleteBlocklistEntry：errors.Is 404/204）
3. 前端：ipam.ts deleteBlocklist/deleteBlocklistEntry + blocklist 页删除按钮（builtin 不显示）

## 顺手修（先前 bug）

`PgBlocklistRepo.List` 的 `ORDER BY created_at` 引用不存在的列（0001_init 实锤 blocklist 表无此列，INSERT 也不用它）——GET /dns/blocklists 一直 500 → 改 `ORDER BY name`。

## 验收标准（可测）

- [x] typecheck/全链绿 + 零外链 PASS
- [x] e2e：GET 列表 200（List 修复实证）；条目删除 204 + 回读消失；列表删除 204 + 回读消失（真验证）
- [x] 文档三件套 + commit [M2-024]

## 实施记录（追加式，勿删旧条目）

### 2026-08-29 · 会话1
- **做了**：spec delete ×2 + gen；BlocklistRepo 接口 2 方法 + errBlocklistNotFound + Mem/PG 双实现；handler DeleteBlocklist/DeleteBlocklistEntry；前端 ipam.ts 2 函数 + blocklist 页删除按钮。
- **顺手修**：PgBlocklistRepo.List ORDER BY created_at → name（先前 bug，GET 列表一直 500——0001_init 实锤表无该列）。
- **验证结果**：typecheck 0 + build:ipam ✓ + sync ✓ + 零外链 PASS + go build/test/lint 全绿；e2e 决定性——GET 列表 200（修复实证）、加条目 201 → 删条目 204 → 回读 0 条、删列表 204 → 回读已消失（GET 200 真验证）。
- **踩坑留痕**：① python 正则插 import 吃掉原首行 `\t"` 前缀致语法错误（net/http" 残骸）——修复后改整行锚定；② 列表回读曾假阳性（GET 500 时 items=[] 恒判「消失」）——修 List 后以 GET 200 真验证。
- **遗留**：条目前端管理区（spec 已有 list/add/delete，前端未实现）P2；列表级 PATCH 编辑（rename 影响 rpz zone 命名链路）P2；builtin 拒删 409 分支未 e2e 覆盖（DB 无 builtin 列表）。
