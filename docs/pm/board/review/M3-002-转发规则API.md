# [M3-002] 转发规则 API（条件转发）

| ID | M3-002 | 状态 | review | 来源 | FR-B-02、§13.4 转发规则 |
|---|---|---|---|---|---|
| 负责 | opencode(backend) | 创建 | 2026-08-26 |

## 目标
forward-zone 规则 CRUD（域名后缀→指定上游、内网根域指向、例外）；dry_run 预检；变更走 unbound reload。

## 验收标准
- [ ] 规则优先级与最长后缀匹配语义单测
- [ ] dryRun 生成校验不下发

## DoD
单测 / lint / FR-B-02 同步 / spec 先行 / commit [M3-002]

## 实施记录

### 2026-08-26 · 会话1
- **做了**：spec 先行——forward-rules 三端点（list/create 含 dryRun 命令预览/update/delete）；迁移 0005；MatchDomain 最长后缀优先（"." 默认兜底最低优先级、大小写/尾点容错、禁用规则不参与）；ForwardService（上游引用校验、dryRun 现库+候选合成预览、创建/更新后重下发）；engine/unbound SyncForwardRules（逐规则 forward_add <domain> <addrs@port>）；Pg/Mem 双仓储；ForwardHandler 接线。
- **验证**：4 新单测全绿（最长后缀矩阵/命令合成/dryRun 不落库/下发触发）；全仓 7 包 lint 0 issues。
- **踩坑留痕**：根域 "." 需特判（否则永远不匹配）；dryRun 预览须含候选规则（预览在持久化前）。
- **遗留**：forward_remove 差量（当前仅 add 收敛，P1）；容器实测在 M3-006。
