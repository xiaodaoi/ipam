# [M4-003] 实时查询流 + 操作审计

| ID | M4-003 | 状态 | review | 来源 | F-R7、FR-F 审计 |
|---|---|---|---|---|---|
| 负责 | opencode(backend) | 创建 | 2026-08-26 |

## 目标
WebSocket/SSE 近实时日志通道（F-R7）；管理员操作审计表+API；AI/Bot 调用者标识区分（§12.3）。

## 验收标准
- [x] live-tail 延迟 ≤2s（500ms 轮询，实测事件 110ms 内抵达）；断线重连（Last-Event-ID 元组续传）
- [x] 审计含 actor 类型（human/bot）与 token 身份（schema+ActorProvider 钩子就绪，M5 JWT 填充真实身份）

## DoD
单测 / lint / §12.3 同步 / spec 先行 / commit [M4-003]

## 实施记录

### 2026-08-27 · 会话1
- **spec 先行**：`GET /logs/tail`（streamLogTail，text/event-stream）+ `GET /audits`（listOperationAudits）+ AuditEntry/AuditPage schema；闸① 0 errors；gen 再生 Go+TS。
- **SSE live-tail**（handler.go serveSSE）：500ms 轮询 Store 滚动窗口 → 新事件按 (ts,mac,domain) 升序发射（DESC 批次倒序），id=游标元组编码可直接续传；15s 心跳保活；Last-Event-ID 优先于 from 参数；per-request 状态全闭包化（无全局）。轮询/心跳节奏可注入供测试。
- **审计域**（audit*.go）：AuditStore 双实现——Mem + Pg（迁移 0009 operation_audit，(ts,id) 元组游标分页）；NewAuditRecorder 中间件统一入账 POST/PATCH/DELETE（resource=路由模板归一、FullPath 空=匿名路由跳过），actor 失败降级 log 不阻断业务。
- **复用**：操作审计查询与 live-tail 均归 日志中心域（logquery 模块），同 M4-002 Store/Service 栈；审计中间件挂在 main.go 路由注册前。
- **验证**：新增 5 组单测全绿（审计过滤矩阵/(ts,id) 翻页无重漏/中间件归一化/SSE smoke@110ms/newerThan 元组判定）；闸② 路由覆盖自动纳入两新端点；golangci-lint 0 issues；全仓 go test 绿。
- **文档**：§12.3 增补审计落地条目（字段契约+M5 接线点+ActorProvider 钩子）。
- **遗留**：① actor 真实身份填充 M5 JWT；② 操作审计 write path 的 dryRun 请求是否入账待 M5 口径统一（当前入账且 status 如实记录）。