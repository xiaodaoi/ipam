# [M3-004] 封禁管控 API（RPZ 名单+策略分组）

| ID | M3-004 | 状态 | review | 来源 | FR-B-11~18、D13、§13.4 封禁管控 |
|---|---|---|---|---|---|
| 负责 | opencode(backend) | 创建 | 2026-08-26 |

## 目标
blocklist/entry CRUD + 订阅源同步 + policy_group(view×cidr×时段) + 增量编译 zonefile + auth_zone_reload 单区刷新。

## 验收标准
- [ ] 50 万条预算的编译管线（增量重写受影响策略组）
- [ ] 订阅源失败保留旧版；去重
- [ ] 拦截统计跳转日志中心（预过滤 blocked）

## DoD
单测+集成 / lint / §5 同步 / spec 先行 / commit [M3-004]

## 实施记录

### 2026-08-26 · 会话1
- **做了**：spec 先行——blocklist 五端点（list/create/sync/entries 查加/policy-groups 查建/compile）+ 502 FEED_DOWN 语义；迁移 0007（pattern 索引）；BlocklistService（订阅源拉取 HTTPSync 30s/5MB→逐行解析去重→版本+1；失败保留旧版；Compile 增量编译：聚合策略组名单条目→BuildRPZZone 去重+action 映射（nxdomain→CNAME . / redirect→CNAME 提示页）→auth_zone_reload 单区刷新）；Pg/Mem 双仓储（ON CONFLICT DO NOTHING 计数实际新增）。
- **验证**：5 新单测全绿（解析去重/动作映射矩阵/失败保旧版/成功去重版本递增/编译返回 zone 信息）；全仓 7 包 lint 0 issues。
- **踩坑留痕**：BlocklistSyncResult.Added 为值类型非指针；derefStr 与 forward_handler 重复定义需收敛；response_ip 条目编译时跳过（qname 才入 RPZ）。
- **遗留**：zonefile 实际写盘+checkconf 在 M3-006 容器实测（当前返回命令串）；订阅源 HTTP 客户端在 compose 网络内的连通性测试随 M3-006。
