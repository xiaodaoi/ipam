# [M2-029] 日志 CSV 导出前端

| 字段 | 内容 |
|---|---|
| ID | M2-029 |
| 状态 | review |
| 来源 | M2-026 扫描注记：/logs/tail SSE 已由 tail 页接入（EventSource ✓ 无缺口），唯一缺口是 /logs/export——spec/后端 CSV 直出（BOM+表头+Content-Disposition），search 页无导出入口 |
| 负责 | opencode(frontend) |
| 创建 | 2026-08-29 |
| 更新 | 2026-08-29 |

## 方案

1. ipam.ts：exportLogsCsv(params)——fetch blob 下载（复用 useAccessStore/BASE，解析 Content-Disposition 文件名，非 JSON 不走 req 封装）
2. search 页：「导出 CSV」按钮——复用当前筛选（hours→from/to + filterType + filterDomain）映射 ExportLogsParams
3. streamLogTail：无缺口（tail 页 EventSource 已接）——卡内关闭该项

## 验收标准（可测）

- [x] typecheck/零外链 PASS
- [x] 容器重建 + /logs/export API 回归（200 + Content-Disposition + CSV BOM/表头/真实数据行）
- [x] 文档三件套 + commit [M2-029]

## 实施记录（追加式，勿删旧条目）

### 2026-08-29 · 会话1
- **做了**：ipam.ts exportLogsCsv（fetch blob 下载：Authorization 复用 accessStore + URLSearchParams + Content-Disposition 文件名解析 + revokeObjectURL）；search 页 onExport（筛选映射 from/to/type/domain）+「导出 CSV」按钮 + message 反馈。
- **验证结果**：typecheck 0 + 产物链 PASS；容器重建 + e2e——GET /logs/export?from=...&exportLimit=100 → 200 + `Content-Disposition: attachment; filename="logs-20260828.csv"` + CSV 首行 BOM/表头（ts,type,severity,...）+ 真实 unbound 数据行。
- **踩坑留痕**：单行 import 块用 `} from '#/api/ipam';` 锚点会切断 LogQuery 前缀（TS1005）——**锚点前必须 sed 确认 import 块是单行还是多行**。
- **遗留**：无——**日志域前端闭环**（检索/TopN/QPS/审计/tail SSE/CSV 导出全入口）。
