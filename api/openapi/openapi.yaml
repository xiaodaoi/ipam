openapi: 3.1.0
info:
  title: IPAM Control Plane API
  version: 0.1.0
  description: |
    IPAM 一体化产品控制面 REST API（唯一事实源，D14 Spec-First）。

    规范要点（架构文档 §12）：operationId 即 AI 工具名，发布后不可改；
    错误模型统一 RFC 9457 Problem；每个 operation 必含描述与成功/错误双示例；
    写操作支持 dry_run 预检与 Idempotency-Key 幂等头。
servers:
  - url: /api/v1
tags:
  - name: platform
    description: 平台能力（系统信息/健康/认证/审计）
  - name: org
    description: 组织分组主数据（多级自定义树，§13.4）
  - name: subnet
    description: DHCPv4/v6 子网与地址池（FR-C、§13.4）
  - name: ledger
    description: 地址台账（六态矩阵与保留/绑定操作，§13.4）
  - name: asset
    description: 资产登记（MAC 身份键，§13.4）
  - name: reservation
    description: 保留与静态绑定批量管理（FR-A-04、§13.4）
  - name: dns
    description: DNS 服务（上游/转发/解析记录/封禁，FR-B、§13.4）
  - name: blocklist
    description: DNS 封禁管控（RPZ 名单/策略分组/订阅源，FR-B-11~18、D13）
paths:
  /dns/settings:
    $ref: './paths/settings.yaml#/SettingsCollection'
  /dns/settings/ttl-overrides:
    $ref: './paths/settings.yaml#/TtlOverridesCollection'
  /dns/cache/flush:
    $ref: './paths/settings.yaml#/CacheFlush'
  /dns/blocklists:
    $ref: './paths/blocklist.yaml#/BlocklistsCollection'
  /dns/blocklists/{listId}:
    $ref: './paths/blocklist.yaml#/BlocklistItem'
  /dns/blocklists/{listId}/entries:
    $ref: './paths/blocklist.yaml#/BlocklistEntriesCollection'
  /dns/policy-groups:
    $ref: './paths/blocklist.yaml#/PolicyGroupsCollection'
  /dns/policy-groups/{groupId}/compile:
    $ref: './paths/blocklist.yaml#/PolicyGroupItem'
  /dns/zones:
    $ref: './paths/dnszone.yaml#/DnsZonesCollection'
  /dns/zones/{zoneId}/records:
    $ref: './paths/dnszone.yaml#/DnsRecordsCollection'
  /dns/zones/{zoneId}/linked:
    $ref: './paths/dnszone.yaml#/DnsZoneLinked'
  /dns/zones/{zoneId}:
    $ref: './paths/dnszone.yaml#/DnsZoneItem'
  /forward-rules:
    $ref: './paths/forward.yaml#/ForwardRulesCollection'
  /forward-rules/{ruleId}:
    $ref: './paths/forward.yaml#/ForwardRuleItem'
  /upstreams:
    $ref: './paths/upstream.yaml#/UpstreamsCollection'
  /upstreams/{upstreamId}:
    $ref: './paths/upstream.yaml#/UpstreamItem'
  /reservations/bulk:
    $ref: './paths/reservation.yaml#/ReservationsBulk'
  /assets:
    $ref: './paths/asset.yaml#/AssetsCollection'
  /assets/{mac}:
    $ref: './paths/asset.yaml#/AssetItem'
  /ledger:
    $ref: './paths/ledger.yaml#/LedgerCollection'
  /ledger/bind:
    $ref: './paths/ledger.yaml#/BindStatic'
  /subnets:
    $ref: './paths/subnets.yaml#/SubnetsCollection'
  /subnets/{subnetId}:
    $ref: './paths/subnets.yaml#/SubnetItem'
  /orgs:
    $ref: './paths/orgs.yaml#/OrgsCollection'
  /orgs/{orgId}:
    $ref: './paths/orgs.yaml#/OrgItem'
  /system/info:
    get:
      operationId: getSystemInfo
      tags: [platform]
      summary: 查询控制面系统信息
      description: |
        返回控制面版本、运行时与就绪状态。
        用途：仪表盘「服务健康」卡片数据源与负载均衡探活。
        所需权限：匿名可读（登录页即需探活）；管理类端点默认 bearerAuth。
      security: []
      responses:
        '200':
          description: 系统信息
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SystemInfo'
              examples:
                ok:
                  summary: 正常运行
                  value:
                    name: ipam-control-plane
                    version: 0.1.0-dev
                    goVersion: go1.27.0
                    now: '2026-08-25T08:00:00Z'
                    ready: true
        '500':
          $ref: '#/components/responses/Problem'
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
      description: 登录颁发的访问令牌；权限 scope 与前端按钮权限码同源定义（§13.2）。
  schemas:
    SystemInfo:
      type: object
      required: [name, version, goVersion, now, ready]
      properties:
        name:
          type: string
          description: 服务标识，固定值 ipam-control-plane
        version:
          type: string
          description: 语义化版本号，构建期经 ldflags 注入
        goVersion:
          type: string
          description: 编译所用 Go 运行时版本（runtime.Version()）
        now:
          type: string
          format: date-time
          description: 服务端当前时间（UTC，RFC3339）
        ready:
          type: boolean
          description: 关键依赖（PostgreSQL）可达则为 true；false 时仪表盘服务健康亮黄
    OrgNode:
      $ref: './components/schemas/org.yaml#/OrgNode'
    OrgTreeNode:
      $ref: './components/schemas/org.yaml#/OrgTreeNode'
    OrgCreate:
      $ref: './components/schemas/org.yaml#/OrgCreate'
    OrgUpdate:
      $ref: './components/schemas/org.yaml#/OrgUpdate'
    Subnet:
      $ref: './components/schemas/subnet.yaml#/Subnet'
    AddressPool:
      $ref: './components/schemas/subnet.yaml#/AddressPool'
    SubnetCreate:
      $ref: './components/schemas/subnet.yaml#/SubnetCreate'
    SubnetUpdate:
      $ref: './components/schemas/subnet.yaml#/SubnetUpdate'
    SubnetList:
      $ref: './components/schemas/subnet.yaml#/SubnetList'
    LedgerState:
      $ref: './components/schemas/ledger.yaml#/LedgerState'
    LedgerRow:
      $ref: './components/schemas/ledger.yaml#/LedgerRow'
    LedgerPage:
      $ref: './components/schemas/ledger.yaml#/LedgerPage'
    ReserveRequest:
      $ref: './components/schemas/ledger.yaml#/ReserveRequest'
    BindRequest:
      $ref: './components/schemas/ledger.yaml#/BindRequest'
    Asset:
      $ref: './components/schemas/asset.yaml#/Asset'
    AssetUpsert:
      $ref: './components/schemas/asset.yaml#/AssetUpsert'
    AssetList:
      $ref: './components/schemas/asset.yaml#/AssetList'
    ReservationBulkEntry:
      $ref: './components/schemas/reservation.yaml#/ReservationBulkEntry'
    ReservationBulkRequest:
      $ref: './components/schemas/reservation.yaml#/ReservationBulkRequest'
    ReservationBulkResult:
      $ref: './components/schemas/reservation.yaml#/ReservationBulkResult'
    Upstream:
      $ref: './components/schemas/upstream.yaml#/Upstream'
    UpstreamCreate:
      $ref: './components/schemas/upstream.yaml#/UpstreamCreate'
    UpstreamUpdate:
      $ref: './components/schemas/upstream.yaml#/UpstreamUpdate'
    UpstreamList:
      $ref: './components/schemas/upstream.yaml#/UpstreamList'
    ForwardRule:
      $ref: './components/schemas/forward.yaml#/ForwardRule'
    ForwardRuleCreate:
      $ref: './components/schemas/forward.yaml#/ForwardRuleCreate'
    ForwardRuleDryRun:
      $ref: './components/schemas/forward.yaml#/ForwardRuleDryRun'
    ForwardRuleList:
      $ref: './components/schemas/forward.yaml#/ForwardRuleList'
    DnsZone:
      $ref: './components/schemas/dnszone.yaml#/DnsZone'
    DnsZoneCreate:
      $ref: './components/schemas/dnszone.yaml#/DnsZoneCreate'
    DnsRecord:
      $ref: './components/schemas/dnszone.yaml#/DnsRecord'
    DnsRecordCreate:
      $ref: './components/schemas/dnszone.yaml#/DnsRecordCreate'
    LinkedRecord:
      $ref: './components/schemas/dnszone.yaml#/LinkedRecord'
    DnsZoneList:
      $ref: './components/schemas/dnszone.yaml#/DnsZoneList'
    DnsRecordList:
      $ref: './components/schemas/dnszone.yaml#/DnsRecordList'
    Blocklist:
      $ref: './components/schemas/blocklist.yaml#/Blocklist'
    BlocklistCreate:
      $ref: './components/schemas/blocklist.yaml#/BlocklistCreate'
    BlocklistEntry:
      $ref: './components/schemas/blocklist.yaml#/BlocklistEntry'
    BlocklistEntryCreate:
      $ref: './components/schemas/blocklist.yaml#/BlocklistEntryCreate'
    BlocklistSyncResult:
      $ref: './components/schemas/blocklist.yaml#/BlocklistSyncResult'
    BlocklistList:
      $ref: './components/schemas/blocklist.yaml#/BlocklistList'
    BlocklistEntryList:
      $ref: './components/schemas/blocklist.yaml#/BlocklistEntryList'
    PolicyGroup:
      $ref: './components/schemas/blocklist.yaml#/PolicyGroup'
    PolicyGroupCreate:
      $ref: './components/schemas/blocklist.yaml#/PolicyGroupCreate'
    RpzCompileResult:
      $ref: './components/schemas/blocklist.yaml#/RpzCompileResult'
    DnsSettings:
      $ref: './components/schemas/settings.yaml#/DnsSettings'
    PerDomainTtl:
      $ref: './components/schemas/settings.yaml#/PerDomainTtl'
    FlushResult:
      $ref: './components/schemas/settings.yaml#/FlushResult'
    Problem:
      type: object
      description: RFC 9457 问题详情。type/code 字段供机器判读，AI agent 可据此自纠重试（§12.2 约定 4）。
      required: [type, title, status]
      properties:
        type:
          type: string
          description: 问题类型 URI，稳定不变供程序匹配
        title:
          type: string
          description: 人类可读摘要
        status:
          type: integer
          description: HTTP 状态码
        detail:
          type: string
          description: 具体原因说明（不含敏感信息）
        code:
          type: string
          description: 业务错误码（如 PLATFORM_DB_DOWN），枚举随端点文档化
        instance:
          type: string
          description: 出错请求的路径
  parameters:
    OrgId:
      $ref: './parameters/org.yaml#/OrgId'
    SubnetId:
      $ref: './parameters/subnet.yaml#/SubnetIdParam'
  responses:
    Problem:
      description: 错误响应（RFC 9457 Problem Details）
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/Problem'
          examples:
            internalError:
              summary: 内部错误典型样例
              value:
                type: 'https://ipam.local/problems/internal'
                title: Internal Server Error
                status: 500
                detail: 数据库暂不可用
                code: PLATFORM_DB_DOWN
                instance: /api/v1/system/info
